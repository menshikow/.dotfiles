general {
        colors = true
        interval = 1
        output_format = "i3bar"
        markup = "none"
        separator = ""
}

order += "tztime local"

tztime local {
        format = "%H:%M:%S"
}
